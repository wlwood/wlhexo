<% if (post.tags && post.tags.length > 0){ %>
  <div class="tags" style="font-size:14px">
    <%- list_tags(post.tags, {
      show_count: false,
      style: 'none',
      class: 'tags'
    }) %>
  </div>
<% } %>
